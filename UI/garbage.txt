HWND second_wnd;
                    WNDCLASS second_wnd_class = {};
                    const char second_wnd_ClassName[] = "second_wndClass";

                    int second_nCmdShow;
                    HINSTANCE second_instance;

                    second_wnd_class.lpfnWndProc   = WindowProc;
                    second_wnd_class.hInstance     = GetModuleHandle(NULL);
                    second_wnd_class.lpszClassName = second_wnd_ClassName;

                    if(!RegisterClass(&second_wnd_class)){
                        MessageBox(NULL, "Window Registration Failed! wc", "Error!",
                        MB_ICONEXCLAMATION | MB_OK);
                        return 0;
                    }

                    cout << "Registration \n";

                    LPCSTR secundary;

                    second_wnd = CreateWindowEx(
                        0,
                        second_wnd_ClassName,
                        "secundary",
                        WS_CHILD,
                        CW_USEDEFAULT, CW_USEDEFAULT, 480, 240,
                        hwnd, NULL, GetModuleHandle(NULL), NULL);

                    cout << "window created\n";

                    SetParent(second_wnd, hwnd);
                    

                    if(second_wnd == NULL){
                        MessageBox(NULL, "Window Creation Failed!", "Error!",
                        MB_ICONEXCLAMATION | MB_OK);
                        cout << "window NULL\n";
                        return 0;
                    }

                    cout << "second wnd not null";

                    ShowWindow(second_wnd, SW_SHOWNORMAL);
                    UpdateWindow(second_wnd);